const { colors } = require('tailwindcss/defaultTheme');

module.exports = {
  theme: {
    extend: {
      colors: {
        golondrino: {
          'default': '#00adef',
          '100': '#d3f3ff',
          'dark': '#046488'
        }
      }
    }
  },
  variants: {},
  plugins: []
}
